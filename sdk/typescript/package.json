{
  "name": "@kyoso/agent",
  "version": "0.1.0",
  "description": "TypeScript SDK for the kyoso scene-agent surface. Types generated from the Rust JSON Schema; pluggable Transport for in-process / subprocess / remote use.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "codegen": "../../scripts/codegen.sh ts",
    "build": "tsc --noEmit",
    "test": "echo 'TS tests TODO' && exit 0"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.0",
    "typescript": "^5.6.0"
  }
}
